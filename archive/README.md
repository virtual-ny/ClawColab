# Archive

This directory preserves more instance-specific examples, history, and real test artifacts that are useful as reference but should not dominate the primary template surface.

## Sections
- `history/` — preserved real workflow artifacts from ClawColab development and testing
- `examples/` — curated examples that may later be promoted back into the main template
