# Demo Spaces

This directory holds project-specific demo spaces that live inside the broader ClawColab coordination repository.

## Current contents
- `btcforecast/` — archived first demo space, now preserved inside `ClawColab/workspace/demo-spaces/`

## Rule
- Keep governance, approvals, role boundaries, and cross-agent coordination in the main `ClawColab` structure.
- Keep project-specific code, notes, data, reports, and notebooks inside each demo-space subdirectory.
